{
  "name": "model-switch",
  "version": "0.3.0",
  "private": true,
  "description": "Local reflex layer for OpenAI Codex: reuse fresh evidence, gate ambiguous checks with TypeSafe Jev, and optionally route models.",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test",
    "check": "node --check src/config.js && node --check src/request.js && node --check src/router.js && node --check src/server.js && node --check src/stats.js && node --check src/upstream.js && node --test"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/aesgalexis/model-switch.git"
  },
  "homepage": "https://github.com/aesgalexis/model-switch#readme",
  "bugs": {
    "url": "https://github.com/aesgalexis/model-switch/issues"
  },
  "keywords": [
    "codex",
    "openai",
    "jev",
    "typesafe",
    "agent-hooks",
    "evidence-cache",
    "semantic-gating",
    "model-routing"
  ],
  "dependencies": {
    "@typesafe-ai/sdk": "0.6.0"
  }
}
